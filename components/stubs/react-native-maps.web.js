// Web stub — react-native-maps uses native-only APIs that don't exist on web.
// The Walk screen shows an OpenStreetMap iframe on web instead.
import { View } from 'react-native';
export default View;
export const Marker  = () => null;
export const Polyline = () => null;
export const PROVIDER_GOOGLE = null;
